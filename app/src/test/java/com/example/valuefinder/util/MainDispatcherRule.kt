package com.example.valuefinder.util

/**
 * Reserved for future coroutine-dispatcher tests.
 *
 * The current unit tests are pure JVM tests and do not require a dispatcher
 * rule, so this file intentionally stays minimal.
 */
class MainDispatcherRule

